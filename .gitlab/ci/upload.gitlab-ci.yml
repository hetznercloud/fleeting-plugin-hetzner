upload-binaries:
  stage: upload
  image: alpine:latest
  extends:
    - .rules:release:all
  dependencies:
    - binaries
  script:
    - apk add curl gnupg make
    - make upload-release
  environment:
    name: upload
  artifacts:
    paths:
      - manifest.txt
